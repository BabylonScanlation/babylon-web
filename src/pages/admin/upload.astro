---
import Layout from '../../layouts/Layout.astro';
// Se elimina la importación duplicada de Header.astro

const sessionCookie = Astro.cookies.get('session');
if (sessionCookie?.value !== 'admin-logged-in') {
  return Astro.redirect('/admin-login');
}
---

<Layout title="Subir Capítulos">
  <main class="admin-container">
    <a href="/admin" class="back-link">&larr; Volver al Panel</a>
    <h1>Subir Capítulos</h1>
    <div class="info-card">
      <h2>Proceso Actual</h2>
      <p>
        Actualmente, los capítulos se suben y procesan automáticamente
        enviando un archivo <code>.zip</code> al canal de Telegram correspondiente a cada serie.
      </p>
      <p>
        El sistema detecta el archivo, lo procesa y lo publica en el sitio.
      </p>
    </div>
  </main>
</Layout>

<style>
  .admin-container { 
    /* --- CORRECCIÓN AQUÍ --- */
    padding: 2rem;
    max-width: 900px; 
    margin: 0 auto;
}
  h1 { margin-bottom: 1.5rem; }
  .back-link { display: inline-block; margin-bottom: 1.5rem;
  color: var(--accent-color);
}
  .info-card {
    background-color: var(--card-background);
    padding: 1.5rem;
    border-radius: 8px;
    line-height: 1.6;
  }
  .info-card h2 { margin-bottom: 1rem; }
  .info-card p { margin-bottom: 0.5rem;
  }
  .info-card code {
    background-color: #333;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-family: monospace;
  }
</style>