---
import AdContainer from '../components/AdContainer.astro';
import FeaturedGrid from '../components/FeaturedGrid.astro';
import HeroCarousel from '../components/HeroCarousel.astro';
import RecentUpdatesGrid from '../components/RecentUpdatesGrid.astro';
import Layout from '../layouts/Layout.astro';
import {
  getAllSeries,
  getHomeData,
  getPopularSeries,
  getRecentSeries,
  getSeriesByChapterCount,
  getSeriesWithRecentChapters,
} from '../lib/data/series';
import { getDB } from '../lib/db';
import { logError } from '../lib/logError';
import { siteConfig } from '../site.config';
import type { Series } from '../types';

const { runtime, user } = Astro.locals;
const r2PublicUrlAssets = runtime.env.R2_PUBLIC_URL_ASSETS || '/api/assets/proxy';

// Astra: El modo NSFW ahora es global vía cookies (logo del header)
const nsfwCookieValue = Astro.cookies.get('babylon_nsfw')?.value;
// Si la cookie existe (true o false), usamos su valor. Si no existe, usamos el perfil.
const allowNsfw = typeof nsfwCookieValue === 'string' 
  ? nsfwCookieValue === 'true' 
  : ((user as any)?.isNsfw || false);

let recentSeries: Series[] = [];
let popularSeries: Series[] = [];
let seriesByChapterCount: Series[] = [];
let seriesWithRecentChapters: any[] = [];
let hasContent = false;

try {
  const db = getDB(runtime.env);

  // Orion: Carga ultra-optimizada en un solo paso
  const homeData = await getHomeData(db, allowNsfw);
  
  recentSeries = homeData.recentSeries as any;
  popularSeries = homeData.popularSeries as any;
  seriesByChapterCount = homeData.seriesByChapterCount as any;
  seriesWithRecentChapters = homeData.seriesWithRecentChapters;
  hasContent = homeData.hasContent;

} catch (error) {
  console.error('🔥 ERROR CRÍTICO EN HOME:', error);
  logError(error, 'Error al obtener las listas de series (Ultra Optimizado)');
}

// Orion: Deduplicación inteligente para el FeaturedGrid
const featuredGridData = [...popularSeries]
  .sort((a, b) => (Number(b.views) || 0) - (Number(a.views) || 0))
  .slice(0, 6);
---

<Layout 
  title={`${siteConfig.name} - Leer Manga, Manhwa y Manhua Online Gratis`}
  description={`La mejor plataforma para leer Manga, Manhwa y Manhua online en español. Capítulos diarios, alta calidad visual y la mejor comunidad. ¡Lee gratis ahora en ${siteConfig.shortName}!`}
>
  <h1 class="sr-only">{siteConfig.name} - Tu plataforma premium para leer Manga, Manhwa y Manhua online gratis</h1>
  {
    hasContent ? (
      <div class="main-content-wrapper">
          {seriesByChapterCount.length > 0 && (
            <HeroCarousel 
              slides={seriesByChapterCount} 
              r2PublicUrlAssets={r2PublicUrlAssets} 
            />
          )}

        <div class="main-sections-container">

          <AdContainer type="bannerLarge" />

          {featuredGridData.length > 0 && (
            <FeaturedGrid mangas={featuredGridData.map((m, i) => ({...m, loading: i < 2 ? 'eager' : 'lazy'}))} />
          )}

          <AdContainer type="bannerSmall" />

          <div class="discovery-zone">
            {seriesWithRecentChapters.length > 0 && (
              <RecentUpdatesGrid
                mangas={seriesWithRecentChapters}
                title="Últimos Capítulos Publicados"
              />
            )}
          </div>

          <AdContainer type="square" />
        </div>
      </div>
    ) : (
      <div class="empty-state-container">
        <img
          src={siteConfig.assets.logoEmpty.startsWith('/') ? `${r2PublicUrlAssets}${siteConfig.assets.logoEmpty}` : siteConfig.assets.logoEmpty}
          alt={`${siteConfig.name} Logo`}
          class="empty-state-logo"
        />
        <p class="empty-state-text">
          Lo sentimos... aún no hay series disponibles.
          <br />
          Vuelva en otro momento.
        </p>
      </div>
    )
  }
</Layout>

<style>
  .main-content-wrapper {
    width: 100%;
  }
  .main-sections-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
    width: 100%;
    box-sizing: border-box;
    overflow-x: hidden;
    /* Astra: Espaciado compacto y elegante */
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .discovery-zone {
    display: flex;
    flex-direction: column;
    gap: 0; /* Astra: Espacio eliminado entre slider y botón */
  }

  .empty-state-container {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
    text-align: center;
    padding: 1rem;
  }

  .empty-state-logo {
    max-width: 500px;
    width: 80%;
    opacity: 0.8;
    filter: drop-shadow(0 0 30px rgba(0, 191, 255, 0.2));
    animation: pulse-logo 4s ease-in-out infinite;
  }

  @keyframes pulse-logo {
    0%, 100% { transform: scale(1); opacity: 0.8; }
    50% { transform: scale(1.05); opacity: 1; }
  }

  .empty-state-text {
    color: #aaa;
    font-size: 1.1rem;
    max-width: 400px;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .main-sections-container { gap: 1.5rem; }
    .library-reveal-section { margin: 0; }
    .premium-reveal-btn { padding: 1rem 2rem; font-size: 0.8rem; }
    .reveal-line { display: none; }
    .reveal-control-box { justify-content: center; }
  }

  :global(body.no-scroll) {
    overflow: hidden !important;
  }
</style>
