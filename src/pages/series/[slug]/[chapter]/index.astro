---
import { env } from 'cloudflare:workers';
import AdContainer from '../../../../components/AdContainer.astro';
import ChapterReader from '../../../../components/ChapterReader.svelte';
import UnifiedComments from '../../../../components/UnifiedComments.astro';
import Layout from '../../../../layouts/Layout.astro';
import { getChapterPayload } from '../../../../lib/data/chapters';
import { getCommentsForTarget } from '../../../../lib/data/comments';
import { getSeriesDetails } from '../../../../lib/data/series';
import { getDB } from '../../../../lib/db';
import { logError } from '../../../../lib/logError';
import { siteConfig } from '../../../../site.config';

const { slug, chapter } = Astro.params;
const user = Astro.locals.user;
const watermark = siteConfig.reader.watermark;

if (!slug || !chapter) return Astro.redirect('/404');

const drizzleDb = getDB(env);

// --- Variables de Estado ---
let chapterPayload: any = null;
let error: string | null = null;
let isAppOnly = false;
let loadingMessage: string | null = null;
let chapterId: number | null = null;
let isProcessing = false;
let allChapters: any[] = [];
let seriesTitle: string | null = null;
let comments: any[] = [];
let seriesData: any = null; // Orion: Variable en scope global para el template
let needsVersionSelection = false;
let availableVersions: any[] = [];

try {
  // 1. Obtener detalles de la serie directamente
  seriesData = await getSeriesDetails(
    drizzleDb,
    slug,
    user ? { uid: user.uid, isAdmin: !!user.isAdmin } : undefined
  );
  if (seriesData) {
    // Astra: Validación de modo NSFW global vía cookies (Header Logo)
    const nsfwCookieValue = Astro.cookies.get('babylon_nsfw')?.value;
    const allowNsfw =
      typeof nsfwCookieValue === 'string' ? nsfwCookieValue === 'true' : user?.isNsfw || false;

    if (seriesData.isNsfw && !allowNsfw) {
      return Astro.redirect('/?nsfw_required=true');
    }

    // Orion: Sanitización ultra-permisiva de fechas para toda la lista de capítulos
    allChapters = (seriesData.chapters || []).map((ch: any) => {
      const ts = ch.createdAt ? new Date(ch.createdAt).getTime() : null;
      return {
        ...ch,
        createdAt: ts && !Number.isNaN(ts) ? ts : null,
      };
    });
    seriesTitle = seriesData.title;
  }

  // 2. Comprobar si hay múltiples versiones de este capítulo
  const chapterNumber = parseFloat(chapter);
  const explicitId = Astro.url.searchParams.get('id');
  const explicitVersion = Astro.url.searchParams.get('version');

  // Extraemos las versiones disponibles para este número de capítulo específico
  availableVersions = allChapters.filter((c: any) => c.chapterNumber === chapterNumber);

  if (availableVersions.length > 1 && !explicitId) {
    // Si hay varias opciones y no se especificó ID, forzamos la pantalla de selección
    let autoSelectId: number | undefined;

    // Auto-selección si viene por query parameter `?version=nsfw` o `?version=sfw`
    if (explicitVersion === 'nsfw') {
      const nsfwVer = availableVersions.find((c) => c.isNsfw);
      if (nsfwVer) autoSelectId = 1;
    } else if (explicitVersion === 'sfw') {
      const sfwVer = availableVersions.find((c) => !c.isNsfw);
      if (sfwVer) autoSelectId = 0;
    }

    if (autoSelectId !== undefined) {
      // Redirigir limpiamente a la URL con ID
      return Astro.redirect(`/series/${slug}/${chapterNumber}?id=${autoSelectId}`);
    }

    // Auto-selección por cookie de preferencia guardada
    if (autoSelectId === undefined) {
      const versionPref = Astro.cookies.get('babylon_version_pref')?.value;
      if (versionPref === 'nsfw') {
        const nsfwVer = availableVersions.find((c) => c.isNsfw);
        if (nsfwVer) autoSelectId = 1;
      } else if (versionPref === 'sfw') {
        const sfwVer = availableVersions.find((c) => !c.isNsfw);
        if (sfwVer) autoSelectId = 0;
      }
    }

    if (autoSelectId !== undefined) {
      return Astro.redirect(`/series/${slug}/${chapterNumber}?id=${autoSelectId}`);
    } else {
      needsVersionSelection = true;
    }
  }

  if (!needsVersionSelection) {
    let targetId = explicitId ? parseInt(explicitId) : undefined;
    if (explicitId === '0') targetId = 0;

    // 3. Obtener payload del capítulo
    const chapterResult = await getChapterPayload(
      drizzleDb,
      env,
      slug,
      chapterNumber,
      targetId !== undefined && !isNaN(targetId) ? { chapterId: targetId } : {},
      Astro.locals.cfContext
    );

    console.log(`[Lector - Debug] Resultado de getChapterPayload:`, {
      exists: !!chapterResult,
      isProcessing: chapterResult?.processing,
      hasPayload: !!chapterResult?.payload,
    });

    if (chapterResult) {
      chapterPayload = chapterResult.payload;
      chapterId = chapterResult.chapterId;
      isProcessing = chapterResult.processing;

      // Orion: Filtrar la lista global por el idioma del capítulo actual para navegación coherente
      const currentLang = chapterResult.chapter?.language || 'es-la';
      allChapters = allChapters.filter((ch: any) => ch.language === currentLang);

      if (isProcessing) {
        loadingMessage = 'Preparando capítulo para lectura...';
      }

      if (chapterId) {
        comments = await getCommentsForTarget(drizzleDb, 'chapter', chapterId, user?.uid);
      }
    } else {
      console.warn(`\n[Lector - Debug] ❌ Capítulo No Encontrado: ${slug}#${chapterNumber}`);
      error = 'El capítulo solicitado no fue encontrado.';
    }
  }
} catch (e) {
  logError(e, 'Error crítico cargando capítulo', { slug, chapter });
  error = 'Error de conexión con el servidor.';
}

// Orion: Evitar que el Edge cachee la página del lector para que las métricas y comentarios sean frescos
Astro.response.headers.set('Cache-Control', 'no-cache, no-store, must-revalidate');

// Lógica para los botones de capítulo siguiente/anterior
const currentChapterNumber = parseFloat(chapter!);

// Filtrar capítulos con números duplicados (ej. múltiples versiones SFW/NSFW)
const uniqueChapters: any[] = [];
const seenChapters = new Set();
for (const ch of allChapters) {
  if (!seenChapters.has(ch.chapterNumber)) {
    seenChapters.add(ch.chapterNumber);
    uniqueChapters.push(ch);
  }
}

const currentChapterIndex = uniqueChapters.findIndex(
  (c) => c.chapterNumber === currentChapterNumber
);

const currentChapter = uniqueChapters.find((c) => c.chapterNumber === currentChapterNumber);
const chapterTitle = currentChapter?.title || '';

let nextChapterUrl: string | null = null;
let nextChapterProp: { slug: string; chapter: string } | null = null;

if (currentChapterIndex > 0) {
  const nextChapter = uniqueChapters[currentChapterIndex - 1];
  if (nextChapter) {
    nextChapterUrl = `/series/${slug}/${nextChapter.chapterNumber}`;
    nextChapterProp = { slug: slug!, chapter: nextChapter.chapterNumber.toString() };
  }
}

let prevChapterUrl: string | null = null;
let prevChapterProp: { slug: string; chapter: string } | null = null;
if (currentChapterIndex !== -1 && currentChapterIndex < uniqueChapters.length - 1) {
  const prevChapter = uniqueChapters[currentChapterIndex + 1];
  if (prevChapter) {
    prevChapterUrl = `/series/${slug}/${prevChapter.chapterNumber}`;
    prevChapterProp = { slug: slug!, chapter: prevChapter.chapterNumber.toString() };
  }
}

// Lógica para navegación inteligente Web-to-Web en caso de error/app-only
let nextWebChapterUrl: string | null = null;
let prevWebChapterUrl: string | null = null;

if (error && currentChapterIndex !== -1) {
  // nextWebChapterUrl: Buscar hacia el futuro (capítulos más nuevos -> índices menores en array DESC)
  for (let i = currentChapterIndex - 1; i >= 0; i--) {
    const ch = uniqueChapters[i];
    if (ch && ch.status === 'live') {
      nextWebChapterUrl = `/series/${slug}/${ch.chapterNumber}`;
      break;
    }
  }

  // prevWebChapterUrl: Buscar hacia el pasado (capítulos más antiguos -> índices mayores en array DESC)
  for (let i = currentChapterIndex + 1; i < uniqueChapters.length; i++) {
    const ch = uniqueChapters[i];
    if (ch && ch.status === 'live') {
      prevWebChapterUrl = `/series/${slug}/${ch.chapterNumber}`;
      break;
    }
  }
}
---

<Layout 
  title={`Leer ${seriesTitle ?? 'Serie'} Capítulo ${chapter} en Español Online - ${siteConfig.name}`} 
  description={`Lee el capítulo ${chapter} de ${seriesTitle} online en español. Disfruta de la mejor experiencia de lectura manga con carga rápida en ${siteConfig.name}.`}
  hidePushAds={true}
  isNsfwContent={seriesData?.isNsfw || false}
>
  {nextChapterUrl && <link rel="next" href={nextChapterUrl} slot="head" />}
  {prevChapterUrl && <link rel="prev" href={prevChapterUrl} slot="head" />}
  <div class="reader-top-nav-wrapper" id="reader-top-nav-wrapper">
    <div class="reader-top-nav-glass">
      <a
        href={`/series/${slug}`}
        class="nav-group back-group"
        title={seriesTitle ?? ''}
      >
        <div class="icon-box">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
        </div>
        <span class="series-title-text">{seriesTitle}</span>
      </a>

      <div class="nav-group chapter-controls">
        <a
          href={prevChapterUrl ?? '#'}
          class:list={['nav-btn', { disabled: !prevChapterUrl }]}
          title="Capítulo Anterior"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
        </a>
        
        <span class="chapter-info">
          <span class="label">CAPÍTULO</span>
          <span class="number">{chapter}</span>
        </span>

        <a
          href={nextChapterUrl ?? '#'}
          class:list={['nav-btn', { disabled: !nextChapterUrl }]}
          title="Capítulo Siguiente"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
        </a>
      </div>
    </div>
  </div>

  <main class="reader-container" id="reader-container">
    {!Astro.locals.isStaff && !user?.isAdmin && siteConfig.ads.enabled && (
      <div class="reader-top-ad">
        <AdContainer type="native" />
      </div>
    )}

    {needsVersionSelection ? (
        <div class="full-screen-error version-selection-container">
            <div class="error-content-glass selection-glass">
                <div class="status-icon-glow app-mode">
                    <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 22h14a2 2 0 0 0 2-2V7.5L14.5 2H6a2 2 0 0 0-2 2v4"></path><polyline points="14 2 14 8 20 8"></polyline><path d="M2 15h10"></path><path d="m9 18 3-3-3-3"></path></svg>
                </div>
                
                <h1 class="error-title">Múltiples Versiones</h1>
                <p class="error-desc">Este capítulo tiene varias opciones disponibles. Por favor, selecciona la versión que deseas leer.</p>
                
                <div class="version-options-grid">
                    {availableVersions.map((v) => (
                        <a 
                          href={`/series/${slug}/${chapter}?id=${v.id}`} 
                          class="version-card"
                          data-version-type={v.isNsfw ? 'nsfw' : 'sfw'}
                          onclick="var cb=document.getElementById('remember-version-pref');if(cb&&cb.checked){document.cookie='babylon_version_pref='+this.getAttribute('data-version-type')+';path=/;max-age=31536000;SameSite=Lax'}"
                        >
                            <div class="version-card-content">
                                <h3 class="version-name">Versión {v.isNsfw ? 'NSFW' : 'SFW'}</h3>
                                {v.scanlationName && <span class="scanlation-name">{v.scanlationName}</span>}
                                {v.isNsfw && <span class="tag-nsfw badge">Sin Censura</span>}
                                {!v.isNsfw && <span class="tag-sfw badge">Censurado</span>}
                            </div>
                            <svg class="version-arrow" viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                        </a>
                    ))}
                </div>

                <label class="remember-pref-label" for="remember-version-pref">
                  <input type="checkbox" id="remember-version-pref" />
                  <span>Recordar mi selección</span>
                </label>

                <div class="action-grid mt-4">
                    <a href={`/series/${slug}`} class="nav-chapter-btn">
                        <span class="sub-label">Volver a la Serie</span>
                    </a>
                </div>
            </div>
        </div>
    ) : error ? (
        <div class="full-screen-error">
            <div class="error-content-glass">
                <div class={`status-icon-glow ${isAppOnly ? 'app-mode' : 'error-mode'}`}>
                    {isAppOnly ? (
                        <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect><path d="M12 18h.01"></path></svg>
                    ) : (
                        <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
                    )}
                </div>
                
                <h1 class="error-title">{isAppOnly ? 'Exclusivo en la App' : 'Capítulo No Disponible'}</h1>
                <p class="error-desc">{error}</p>
                
                <div class="action-grid">
                    {prevWebChapterUrl && (
                        <a href={prevWebChapterUrl} class="nav-chapter-btn prev" title="Capítulo Anterior (Web)">
                            <span class="sub-label">Anterior Web</span>
                            <svg class="icon-arrow" viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
                        </a>
                    )}

                    {isAppOnly ? (
                        <a href="/app" class="primary-cta-btn">
                            <span class="btn-icon">📱</span>
                            <div class="btn-text-group">
                                <span class="main-text">Descargar App</span>
                                <span class="sub-text">Leer sin límites</span>
                            </div>
                        </a>
                    ) : (
                        <a href={`/series/${slug}`} class="primary-cta-btn">
                            <span class="btn-icon">📚</span>
                            <div class="btn-text-group">
                                <span class="main-text">Volver a la Serie</span>
                                <span class="sub-text">Explorar capítulos</span>
                            </div>
                        </a>
                    )}

                    {nextWebChapterUrl && (
                        <a href={nextWebChapterUrl} class="nav-chapter-btn next" title="Capítulo Siguiente (Web)">
                            <span class="sub-label">Siguiente Web</span>
                            <svg class="icon-arrow" viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                        </a>
                    )}
                </div>
            </div>
        </div>
    ) : (
        <div 
          id="reader-data-bridge" 
          data-slug={slug}
          data-chapter={chapter}
          data-payload={JSON.stringify(chapterPayload)}
          data-chapter-id={chapterId}
          data-series-id={seriesData?.id}
          data-processing={isProcessing ? 'true' : 'false'}
          data-loading-msg={loadingMessage}
          data-series-title={seriesTitle}
          data-watermark={watermark}
          style="display: none;"
        ></div>
        <ChapterReader 
          client:load 
          slug={slug || ''}
          chapter={chapter || ''}
          chapterPayload={chapterPayload}
          chapterId={chapterId || undefined}
          processing={isProcessing}
          initialLoadingMessage={loadingMessage || undefined}
          seriesTitle={seriesTitle || undefined}
          chapterTitle={chapterTitle}
          watermark={watermark}
          nextChapter={nextChapterProp || undefined} 
          prevChapter={prevChapterProp || undefined}
          isStaff={Astro.locals.isStaff}
        />
    )}
  </main>

  {
    chapterId && (
      <div class="comments-wrapper" id="comments-wrapper">
        <div class="comments-container-glass">
          <UnifiedComments targetType="chapter" targetId={chapterId} comments={comments} user={user ?? null} />
        </div>
      </div>
    )
  }
</Layout>

<script>
  // Astra: Mostrar Nav solo en la zona de comentarios y marcar el contexto
  const commentsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        document.body.setAttribute('data-show-nav', 'true');
        document.body.setAttribute('data-in-comments', 'true');
      } else {
        document.body.removeAttribute('data-show-nav');
        document.body.removeAttribute('data-in-comments');
      }
    });
  }, { threshold: 0.1 });

  const commentsWrapper = document.getElementById('comments-wrapper');
  if (commentsWrapper) {
    commentsObserver.observe(commentsWrapper);
  }

  // Astra: Lógica de ocultación para el Header Principal
  let lastScrollY = window.scrollY;
  const siteHeader = document.querySelector('.site-header');

  window.addEventListener('scroll', () => {
    if (!siteHeader) return;
    // Si el modal está abierto, no hacer nada
    if (document.body.getAttribute('data-reader-modal') === "open") return;

    const currentScrollY = window.scrollY;
    if (currentScrollY > lastScrollY && currentScrollY > 80) {
      siteHeader.classList.add('hidden');
    } else {
      siteHeader.classList.remove('hidden');
    }
    lastScrollY = currentScrollY;
  }, { passive: true });
</script>

<style is:global>
  /* Astra: Ocultar nav por defecto en el lector, pero mostrarla en comentarios */
  .mobile-bottom-nav {
    display: block !important;
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1) !important;
    pointer-events: none;
  }

  body[data-show-nav="true"] .mobile-bottom-nav {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  /* Astra: Estilos para la ocultación del Header Principal */
  .site-header {
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .site-header.hidden {
    transform: translateY(-100%) !important;
  }

  /* Si el modal está abierto, la regla del Layout.astro (z-index 10000) mandará */
</style>

<style>
  .reader-container {
    width: 100%;
    max-width: 100%;
    min-height: 80vh;
  }
  .comments-wrapper {
    margin: 2rem auto;
    width: 100%;
    max-width: 1200px;
    padding: 0 1rem;
  }

  .reader-top-ad,
  .reader-bottom-ad,
  .reader-footer-ad {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .reader-top-ad {
    margin-bottom: 0;
    max-height: 280px;
    overflow: hidden;
    display: flex;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .reader-top-ad {
      margin-top: 1.5rem;
      margin-bottom: 1.5rem;
    }
  }

  .reader-top-ad :global(.ad-container-root),
  .reader-top-ad :global(.ad-inner-wrapper),
  .reader-top-ad :global([class*="__stand"]) {
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
  }

  .reader-top-ad :global(.ad-container-root) {
    max-height: 280px !important;
    min-height: 90px !important;
  }

  .reader-bottom-ad {
    margin-top: 1.5rem;
  }

  .reader-footer-ad {
    margin-top: 2rem;
    margin-bottom: 2rem; /* Astra: Espaciado extra para el footer en móviles */
  }

  :global(header),
  .reader-top-nav-wrapper {
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1) !important;
    touch-action: pan-y pinch-zoom;
    /* will-change removed for memory optimization */
  }
  :global(header.hidden) {
    transform: translateY(-100%);
  }
  .reader-top-nav-wrapper.hidden {
    transform: translateY(calc(-100% - 80px));
  }
  
  .reader-top-nav-wrapper {
    position: sticky;
    top: 0; /* Astra: Ahora se pega al techo absoluto */
    z-index: 800;
    width: 100%;
    padding: 10px 1rem 5px 1rem;
    pointer-events: none; /* Let clicks pass through empty areas */
  }

  .reader-top-nav-glass {
    pointer-events: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    
    background: rgba(18, 18, 18, 0.6);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: none; /* Astra: Eliminado borde que causaba líneas blancas */
    box-shadow: none; /* Astra: Eliminada sombra que podía parecer una línea */
    
    padding: 0.5rem 0.75rem;
    border-radius: 16px;
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
    transition: all 0.3s ease;
  }

  .nav-group {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  /* Back Button Group */
  .back-group {
    text-decoration: none;
    color: #fff;
    transition: opacity 0.2s;
    max-width: 60%;
    overflow: hidden;
  }
  .back-group:hover {
    opacity: 0.8;
  }
  
  .icon-box {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 10px;
    color: var(--accent-color);
    flex-shrink: 0;
  }

  .series-title-text {
    font-weight: 700;
    font-size: 0.95rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Chapter Controls */
  .chapter-controls {
    background: rgba(0, 0, 0, 0.2);
    padding: 4px;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .nav-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    color: #ccc;
    transition: all 0.2s;
  }
  .nav-btn:hover:not(.disabled) {
    background: rgba(255, 255, 255, 0.1);
    color: #fff;
  }
  .nav-btn.disabled {
    opacity: 0.3;
    cursor: default;
  }

  .chapter-info {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0 0.5rem;
    line-height: 1;
  }
  
  .chapter-info .label {
    font-size: 0.6rem;
    font-weight: 800;
    color: #555;
    letter-spacing: 0.1em;
  }
  
  .chapter-info .number {
    font-size: 1rem;
    font-weight: 800;
    color: #fff;
    font-variant-numeric: tabular-nums;
  }
  
  @media (max-width: 768px) {
    .reader-top-nav-wrapper {
      top: 0; /* Astra: Pegado al techo absoluto en móvil */
      padding: 5px 10px;
    }
    .reader-top-nav-glass {
      padding: 0.4rem;
      border-radius: 12px;
    }
    .series-title-text {
      font-size: 0.85rem;
      max-width: 150px;
    }
    .chapter-info .label { display: none; }
    .chapter-info .number { font-size: 0.9rem; }
    .icon-box { width: 32px; height: 32px; }
    .nav-btn { width: 28px; height: 28px; }
  }

  /* --- Estilos de Pantalla de Error (Capítulo No Disponible / App) --- */
  .full-screen-error {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    padding: 2rem 1rem;
    position: relative;
    z-index: 10;
  }

  .error-content-glass {
    background: rgba(18, 18, 18, 0.7);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 24px;
    padding: 3rem 2rem;
    max-width: 480px;
    width: 100%;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
    animation: fadeUpIn 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .selection-glass {
    max-width: 560px;
  }

  .version-options-grid {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    margin-bottom: 2rem;
  }

  .version-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.03), rgba(0, 0, 0, 0.2));
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: 1.25rem;
    border-radius: 16px;
    text-decoration: none;
    color: inherit;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .version-card:hover {
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.08), rgba(0, 0, 0, 0.3));
    border-color: rgba(99, 102, 241, 0.4);
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  }

  .version-card-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
  }

  .version-name {
    font-size: 1.1rem;
    font-weight: 700;
    margin: 0;
    color: #fff;
  }

  .scanlation-name {
    font-size: 0.8rem;
    color: #a0a0a0;
  }

  .badge {
    font-size: 0.7rem;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .tag-nsfw {
    background: rgba(255, 71, 87, 0.15);
    color: #ff4757;
    border: 1px solid rgba(255, 71, 87, 0.3);
  }

  .tag-sfw {
    background: rgba(46, 213, 115, 0.15);
    color: #2ed573;
    border: 1px solid rgba(46, 213, 115, 0.3);
  }

  .version-arrow {
    color: rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
  }

  .version-card:hover .version-arrow {
    color: #fff;
    transform: translateX(4px);
  }

  .mt-4 {
    margin-top: 1rem;
  }

  .remember-pref-label {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    padding: 0.6rem 1rem;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    margin-bottom: 1rem;
    transition: all 0.2s ease;
  }
  .remember-pref-label:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.12);
  }
  .remember-pref-label input[type="checkbox"] {
    width: 18px;
    height: 18px;
    accent-color: var(--accent-color, #00bfff);
    cursor: pointer;
  }
  .remember-pref-label span {
    color: #aaa;
    font-size: 0.85rem;
    font-weight: 600;
  }

  @keyframes fadeUpIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .status-icon-glow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin-bottom: 1.5rem;
    position: relative;
  }

  .status-icon-glow::before {
    content: '';
    position: absolute;
    inset: -20px;
    border-radius: 50%;
    filter: blur(30px);
    opacity: 0.4;
    z-index: -1;
  }

  .status-icon-glow.error-mode {
    color: #ff4757;
    background: rgba(255, 71, 87, 0.1);
  }
  .status-icon-glow.error-mode::before {
    background: #ff4757;
  }

  .status-icon-glow.app-mode {
    color: var(--accent-color);
    background: rgba(var(--accent-color-rgb, 0, 191, 255), 0.1);
  }
  .status-icon-glow.app-mode::before {
    background: var(--accent-color);
  }

  .error-title {
    font-size: 1.75rem;
    font-weight: 800;
    color: #ffffff;
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }

  .error-desc {
    font-size: 1rem;
    color: #a0a0a0;
    margin-bottom: 2.5rem;
    line-height: 1.5;
    max-width: 90%;
  }

  .action-grid {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    width: 100%;
    flex-wrap: wrap;
  }

  .primary-cta-btn {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 0.75rem 1.5rem;
    border-radius: 16px;
    text-decoration: none;
    color: #fff;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    flex-grow: 1;
    justify-content: center;
  }

  .primary-cta-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .btn-icon {
    font-size: 1.5rem;
  }

  .btn-text-group {
    display: flex;
    flex-direction: column;
    text-align: left;
  }

  .main-text {
    font-weight: 700;
    font-size: 0.95rem;
  }

  .sub-text {
    font-size: 0.75rem;
    color: #888;
    font-weight: 500;
  }

  .nav-chapter-btn {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.25rem;
    background: transparent;
    border: none;
    color: #888;
    text-decoration: none;
    padding: 0.5rem;
    border-radius: 12px;
    transition: all 0.2s;
  }

  .nav-chapter-btn:hover {
    color: #fff;
    background: rgba(255, 255, 255, 0.05);
  }

  .nav-chapter-btn .sub-label {
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 700;
  }

  @media (max-width: 480px) {
    .action-grid {
      flex-direction: column;
    }
    .primary-cta-btn {
      width: 100%;
    }
    .nav-chapter-btn {
      flex-direction: row;
      width: 100%;
      background: rgba(255, 255, 255, 0.02);
    }
  }
</style>
