import type { APIRoute } from 'astro';
import { siteConfig } from '../site.config';

export const GET: APIRoute = async () => {
  const siteUrl = siteConfig.url;

  // Formato compatible con Mangayomi (JS Extensions + Mihon Style)
  const extensions = [
    {
      id: 'babylon-official',
      name: siteConfig.name,
      pkg: `com.${siteConfig.shortName.toLowerCase()}.scanlation`,
      version: '0.2.1',
      lang: 'es',
      site: siteUrl,
      baseUrl: siteUrl,
      apiUrl: siteUrl,
      iconUrl: `${siteUrl}${siteConfig.assets.favicon}`,
      url: `${siteUrl}/repo/extension.js`,
      typeSource: 'single',
      nsfw: 0,
      itemType: 0,
      sourceCodeLanguage: 1,
      appMinVerReq: '0.1.0',
      // Campos extra para simular repo estilo Keiyoushi si es necesario
      sources: [{ name: siteConfig.name, id: 'babylon-official', baseUrl: siteUrl, lang: 'es' }]
    },
    {
      id: 'hitomi-la-es',
      name: 'Hitomi.la',
      pkg: "com.hitomi.la.extension",
      version: '1.0.1',
      lang: 'es',
      site: 'https://hitomi.la',
      baseUrl: 'https://hitomi.la',
      apiUrl: '',
      iconUrl: 'https://hitomi.la/favicon.ico',
      url: `${siteUrl}/repo/hitomi.js`,
      typeSource: 'single',
      nsfw: 1,
      itemType: 0,
      sourceCodeLanguage: 1,
      appMinVerReq: '0.1.0',
      sources: [{ name: 'Hitomi.la', id: 'hitomi-la-es', baseUrl: 'https://hitomi.la', lang: 'es' }]
    }
  ];

  return new Response(JSON.stringify(extensions), {
    headers: {
      'Content-Type': 'application/json',
      'Access-Control-Allow-Origin': '*',
    },
  });
};
